# Backend runtime configuration
APP_ENV=production
APP_VERSION=0.1.0-mvp
SECRET_KEY=replace-me-in-render
CORS_ORIGINS=*
DEMO_API_TOKEN=demo-token-please-change

# Stripe (optional, leave blank if not using billing)
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=
STRIPE_BILLING_PORTAL_URL=

# Frontend (build-time). On Render this is wired via fromService.envVarKey: RENDER_EXTERNAL_URL
VITE_API_BASE_URL=
